#include <bits/stdc++.h>

class FormationParser  {
    public:
        FormationParser() = default;
        ~FormationParser() = default;

        std::string readBeforeKickOffFormation(int playerUnum);
};